set_static:
  fields:
    dashboard:
      required: true
      selector:
        text:
      example: default
    static_path:
      required: true
      selector:
        text:
      example: www/backgrounds/default.jpg

refresh:
  description: Refreshes the configured dashboard background. For Unsplash, this downloads a new image and updates the local public background URL.
  fields:
    dashboard:
      required: true
      selector:
        text:
      example: default
    url:
      required: false
      selector:
        text:
      example: https://example.com/background.jpg
